# 2026-08-19 工作日志

## Google Play Billing 支付系统接入

- 在 D:\GodotProjects\godot_learning\MySpire\Client\ 现有客户端基础上接入 Google Play Billing
- 新增文件（4 个）：
  - scripts/autoload/PaymentManager.gd — 支付核心，封装 GodotGooglePlayBilling 插件 + 桌面模拟器
  - scripts/ui/screens/ShopScreen.gd — 商店画面 UI（商品卡片 + 购买流程 + 恢复购买）
  - assets/content/iap_products.json — 6 个商品定义（2 非消耗 + 3 消耗 + 1 装饰）
  - android/plugins/README.md — GodotGooglePlayBilling 插件安装指南
- 修改文件（6 个）：
  - project.godot — 注册 PaymentManager autoload + [android] modules 配置
  - ContentRegistry.gd — 加 IAP 商品加载与查询（get_iap_product / get_iap_by_category）
  - SaveManager.gd — 加 IAP 独立存档（user://myspire_iap.json），非消耗品记录 + 消耗品库存
  - NetworkClient.gd — 加 verify_purchase_receipt 验证桩（离线信任，上线前改强制在线）
  - GameManager.gd — SCREENS 加 shop 画面
  - MainMenu.gd — 加"商店"入口按钮
- 新增 ADR：
  - ADR-009: 桌面模拟器 + Android 插件双轨（OS.get_name() 检测平台，桌面走模拟器，Android 走真插件）
  - ADR-010: IAP 存档独立于 Run 存档（myspire_iap.json vs myspire_run.json）
- 消耗品子类型区分：reward_gold 字段 = 即时生效（不加库存直接转化金币），无此字段 = 库存型（add_consumable）
- 支付系统设计文档已写入 KnowledgeBase/杀戮尖塔_支付系统设计.md
- Client 工程文件数从 26 增至 30
- overview.md 已更新（ADR 表 + 目录结构 + 已完成/待开始清单）

## Apple StoreKit (iOS) 支付接入

- 在现有 PaymentManager 基础上扩展为三轨支付：Android (GodotGooglePlayBilling) + iOS (InAppStore) + 桌面模拟器
- 修改文件（4 个）：
  - PaymentManager.gd — 重写：新增 is_ios 检测、InAppStore 单例接入、iOS 信号回调（product_info/purchase_success/purchase_failed/purchase_cancel/restore_finished）、SKU↔Apple ID 翻译层、finish_transaction 映射为 consume/acknowledge
  - iap_products.json — 每个商品增加 apple_id 字段（com.myspire.* reverse-DNS 格式）
  - ContentRegistry.gd — 新增 get_apple_id(sku) / get_sku_by_apple_id(apple_id) 双向查询
  - NetworkClient.gd — verify_purchase_receipt 按 purchase.platform 路由 iOS receipt vs Android signature
  - project.godot — [android] 段后追加 iOS 导出配置注释
- 新增 ADR-011: 三轨支付抽象 + SKU 双向映射（内部 SKU 统一标识，平台 ID 自动翻译，归一化 purchase Dictionary 跨平台统一处理）
- iOS 购买流程关键差异：finish_transaction 无回调（call_deferred 触发 _on_purchase_consumed）；应用启动自动 restore（Apple 最佳实践）
- 支付系统设计文档已更新：新增 iOS 导出配置章节（8.1-8.4）、平台 ID 对照表、归一化字段映射表、安全考量补充 iOS receipt 验证
- InAppStore 单例内置于 Godot 4 iOS 导出模板，无需额外插件（与 Android 需手动安装 GodotGooglePlayBilling 不同）
- ShopScreen.gd 无需修改——统一信号接口保证 UI 层跨平台无感知

## iOS / Android 打包构建系统

- 在 Client/ 下新增完整的打包构建系统
- 新增文件（9 个）：
  - export_presets.cfg — 3 个导出预设（Android Debug APK + Android Release AAB + iOS Xcode Project）
  - .gitignore — 忽略构建产物 + 签名文件（.keystore/.p12/.mobileprovision 等）
  - build/generate_icons.py — Python 脚本，用 Pillow 从 icon.svg 设计生成全部平台 PNG 图标
  - build/generate_keystore.bat — Android release keystore 签名生成器（keytool）
  - build/build_android.bat — Android 构建脚本（debug APK + release AAB，自动检测 Godot CLI）
  - build/build_ios.sh — iOS 构建脚本（export/ipa/archive，macOS 运行）
  - build/build_all.bat — 全平台一键构建入口
  - assets/icons/android/ — 3 个 Android 图标（192 主图标 + 432 自适应前景/背景）
  - assets/icons/ios/ — 11 个 iOS 图标（40-1024 全尺寸）
- 修改文件（3 个）：
  - overview.md — 已完成清单加"打包构建系统"；目录结构加 build/ + assets/icons/
  - Client/README.md — 新增"打包构建"章节，引用构建指南
  - KnowledgeBase/README.md — 文档清单加第 07 条"打包构建指南"
- 新增文档：KnowledgeBase/杀戮尖塔_打包构建指南.md — 前置依赖/导出预设/Android流程/iOS流程/一键命令/图标管理/常见问题排查
- export_presets.cfg 关键配置：
  - Android: com.myspire.game, min_sdk=24, target_sdk=34, arm64-v8a, Google Play Billing 插件, INTERNET+BILLING+VIBRATE 权限
  - iOS: com.myspire.game, min_ios_version=14.0, portrait only, in_app_purchases=true, SKPaymentPrivacy plist
  - iOS info.plist 内嵌 NSLocalNetworkUsageDescription（联机权限）
- 图标生成器使用 Pillow 绘制：径向渐变背景(#080814→#1a2f5c) + 金色菱形轮廓(#c9a84c) + 内菱形填充(85% opacity)
- 构建脚本统一 Godot CLI 检测：GODOT_PATH 环境变量 → PATH → 常见安装路径
- Client 工程文件数从 32 增至 41（+9 新文件，不含生成的 PNG 图标）
