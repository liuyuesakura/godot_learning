# 2026-08-21 工作日志

## Android Debug APK 首次出包成功 ✅

产物：`D:\GodotProjects\godot_learning\MySpire\Client\build\dist\MySpire-debug.apk`（80.2MB，arm64-v8a，含 GodotGooglePlayBilling 插件）

### 完整的踩坑链与解法（已全部固化到《杀戮尖塔_打包构建指南.md》7.2b 节）

1. **导出模板下载**：GitHub/CDN 不可达 → 写 Python 脚本用 HTTP Range 请求从 .tpz（1.28GB）中只提取 3 个 Android 模板文件（~440MB），经 gh-proxy.com 代理下载。脚本支持断点续传 + CRC32 校验（`download_android_source.py` v2）
2. **模板安装路径**：模板文件放 `%APPDATA%\Godot\export_templates\4.7.1.stable.mono\templates\`；`android_source.zip` 还需复制一份到版本目录根
3. **项目内 Gradle 构建模板**：手动解压 android_source.zip → `Client/android/build/`（等价 Project → Install Android Build Template）
4. **`.build_version` 位置陷阱**：必须放 `Client/android/.build_version`（android/ 目录，不是 android/build/ 里！）——源码逻辑是 `gradle_build_directory.get_base_dir().path_join(".build_version")`，内容为 `4.7.1.stable.mono`
5. **Gradle 发行包超时**：services.gradle.org 不可达 → `gradle-wrapper.properties` 的 distributionUrl 改腾讯镜像
6. **JDK 版本冲突（关键）**：Android Studio JBR 是 JDK 25，Gradle 8.11 报 `Unsupported class file major version 69` → 下载 Temurin JDK 21 便携版（清华镜像）到 `C:\Users\amerhau\.jdks\jdk-21.0.12+8`，用户级 JAVA_HOME + Godot 编辑器设置 java_sdk_path 均指向它。**Android 打包 JDK 兼容区间：17~21**
7. **Maven 镜像**：`~/.gradle/init.gradle` 配阿里云三仓库镜像

### 环境变量（用户级，已持久化）

- `JAVA_HOME` = `C:\Users\amerhau\.jdks\jdk-21.0.12+8`（从 JBR 改为 JDK 21）
- `ANDROID_HOME` / `ANDROID_SDK_ROOT` = `C:\Users\amerhau\AppData\Local\Android\Sdk`
- `GODOT_PATH` = `D:\Program Files\Godot_v4.7.1-stable_mono_win64\Godot_v4.7.1-stable_mono_win64_console.exe`

### 导出命令（已验证）

```bash
export JAVA_HOME="C:/Users/amerhau/.jdks/jdk-21.0.12+8"
"$GODOT" --headless --export-debug "Android Debug (APK)" --path "D:/GodotProjects/godot_learning/MySpire/Client"
```

### 其他

- Godot headless 导出完成后进程不自动退出（已知行为），APK 产出后可手动终止
- 首次 Gradle 构建约 10 分钟（含依赖下载），后续构建快
- export_presets.cfg 中 target_sdk 已改为 37（匹配本机 SDK）
